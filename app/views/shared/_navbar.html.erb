<nav class="navbar navbar-expand-lg navbar-light">
  <div class="container-fluid">
    <%= link_to "My Clinic", root_path, class: "navbar-brand" %>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNavAltMarkup" aria-controls="navbarNavAltMarkup" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarNavAltMarkup">
      <div class="navbar-nav">
        <% if current_user.doctor? %>
          <%= link_to "Profile", doctor_path(current_user), class: "nav-link" %>
        <% else %>
          <%= link_to "Profile", patient_path(current_user), class: "nav-link" %>
          <%= link_to "Doctors List", doctors_path, class: "nav-link" %>
        <% end %>
        <%= link_to "My Appointments", appointments_path, class: "nav-link" %>
      </div>
    </div>
    <div class="align-self-end">
      <%= button_to "Sign Out", destroy_user_session_path, method: :delete, class: "btn btn-primary" %>
    </div>
  </div>
</nav>
